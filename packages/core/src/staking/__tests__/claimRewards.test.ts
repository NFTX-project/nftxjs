import claimRewards from '../claimRewards';

describe('when there is 1 vault', () => {
  it.todo('claims vault rewards');
});

describe('when there are multiple vaults', () => {
  it.todo('claims multiple rewards');
});

describe('when there are no vaults', () => {
  it.todo('throw an error');
});
