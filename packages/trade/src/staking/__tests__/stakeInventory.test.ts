import stakeInventory from '../stakeInventory';

describe('when standard is 721', () => {
  it.todo('provides inventory');
  it.todo('passes all token ids');
});

describe('when standard is 1155', () => {
  it.todo('provides inventory');
  it.todo('passes all token ids and amounts');
});
