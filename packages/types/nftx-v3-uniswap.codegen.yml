schema: https://api.thegraph.com/subgraphs/name/gundamdweeb/nftx-amm
generates:
  ./src/subgraph/nftx-v3-uniswap.ts:
    config:
      useTypeImports: true
      strictScalars: true
      scalars:
        Bytes:
          input: string
          output: string
        BigInt:
          input: string
          output: string
        BigDecimal:
          input: string
          output: string
        Int8: string|number
    plugins:
      - typescript
